# 数据库模型与迁移计划 v2

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v2.6
**日期**: 2026-03-23

## 0. 适用范围

1. 本文件同时服务 App 业务数据模型与 `TestFlight` 共用的正式后端数据模型。
2. 现行目标态为新购杭州 `RDS PostgreSQL Serverless` 的关系型表模型，不再采用 EMAS 集合模型。
3. Web 管理后台现行通过 `admin.*` RPC 与同库 `admin_*` 表工作，和 App 业务表共享同一后端实例。
4. 历史 Python 后端表结构仅作为参考，本期不迁移旧数据。

## 1. 现行目标表

- `users`
- `sessions`
- `teams`
- `team_members`
- `join_requests`
- `tournaments`
- `matches`
- `match_rosters`
- `messages`
- `schedule_sources`
- `media_files`
- `counters`
- `user_auth_identities`
- `sms_verification_codes`
- `admin_users`
- `admin_refresh_tokens`
- `admin_audit_logs`

## 2. 兼容参考模型

- 历史 Python 后端中的 `refresh_tokens`、`team_join_requests`、`tournament_participants`、`user_message_status` 仍可作为迁移参考。
- `admin_users`、`admin_refresh_tokens`、`admin_audit_logs` 属于现行 Web 管理后台必需结构；`admin_sessions` 仍不单独建表，统一复用 refresh token 机制。

## 3. 关键约束

1. `join_requests`：
   - 同一申请人对同一队伍仅允许 1 条 `pending` 记录。
   - 已通过/已拒绝不影响再次申请。

2. `match_rosters`：
   - 同一 `match_id + team_id + position` 只允许一条有效记录。
   - 保证同一队伍同一场次不会重复辩位。

3. 主键与公开标识：
   - 主键统一 UUID 或等价全局唯一标识。
   - 用户/队伍对外使用 `public_id`。

4. `user_auth_identities`：
   - 统一承载账号身份来源，`provider` 初始支持 `apple` 与 `phone`。
   - `provider_subject` 对 `apple` 存储 Apple `sub`，对 `phone` 存储标准化后的中国大陆 E.164 手机号。
   - `(provider, provider_subject)` 必须唯一，避免同一身份重复绑定。
   - 账号删除时应释放对应身份记录，V1 不做 Apple 与手机号的自动合并。

5. `sms_verification_codes`：
   - 负责记录验证码发送、有效期、尝试次数、核验状态与服务端持久化的验证码摘要。
   - 同一手机号在 resend interval 内不得重复创建新的有效验证码记录，重复发送应返回 `PHONE_CODE_TOO_FREQUENT`。
   - 校验成功后写入 `verified_at`，并由服务端本地摘要核验决定成功与否；provider 仅负责短信下发。

6. `users` 账号删除：
   - 使用 `status = 1 (DELETED)` 表示账号已删除。
   - 新增 `deleted_at` 记录删除时间。
   - 删除时释放当前账号关联的 Apple / 手机号身份绑定，允许同一 Apple 账号或同一手机号重新注册新账号。
   - 软删除后保留最小必要审计数据，不恢复旧账号、不迁移旧数据。

7. `sessions` 会话撤销：
   - 账号删除时必须批量写入 `revoked_at`。
   - 删除账号后刷新会话请求必须失效。

8. `messages`：
   - 接收人维度按 `created_at` 倒序读取。
   - `ack` 行为只更新目标消息的已读状态，不改写历史内容。

9. `counters`：
   - 负责生成用户/队伍等公开标识的顺序号。
   - 不要求历史数值迁移，允许迁移期从空库重新计数。

10. 测试账号与命名规范：
   - `TestFlight` 共享正式数据库，但必须使用专用测试账号、测试队伍和测试赛事命名规范。
   - 测试数据的公开展示名应一眼可识别用途，避免污染正式数据。

11. `admin_*`：
   - `admin_users.email` 必须唯一，默认本地种子管理员允许通过环境变量幂等覆盖。
   - `admin_refresh_tokens.token_jti` 必须唯一，登出或轮换后需可撤销。
   - `admin_audit_logs` 需记录资源类型、资源 ID、动作、前后快照，覆盖用户/队伍/赛事/场次写操作。
   - 后台关系型写链路现行直接落在 `team_join_requests`、`team_members`、`tournament_participants`、`match_rosters` 与 `matches` 上，不再要求运营侧手工改裸 UUID。

12. `teams`：
   - `created_at` 作为队伍创建时间戳保留在模型中，客户端与契约口径据此做默认“我方队伍”排序。
   - 本期仅补齐 iOS DTO 与契约引用，不再新增数据库字段。

13. `team_members`：
   - 新增 `display_name`，语义为成员在当前队伍内使用的队内称呼。
   - 允许为空；为空时客户端与接口层回退显示账号全局昵称。
   - 入队申请表单中的“队内称呼”审批通过后，应默认写入该字段。

## 4. 迁移策略

1. 第一步：新购杭州 `RDS PostgreSQL Serverless`，开启 `AutoPause`，按最小配置创建空 schema 与索引。
2. 第二步：补齐身份表与验证码表，先让手机号 mock 登录在本地可走通。
3. 第三步：写入最小种子（调试用户、样例队伍、默认 schedule source）。
4. 第四步：iOS 与后端联调，优先走 `healthz/auth/users/teams` 与手机号登录闭环。
5. 第五步：补齐 `tournaments/messages/schedule/media` 并在 `TestFlight` 上复用同一套正式后端。

## 5. 回滚策略

- 新库在正式切流前不承载历史数据，因此优先采用“重建资源 + 重放种子”的回滚方式。
- 旧 RDS 在新库可用前继续保留，便于必要时回退到旧后端读取路径。
- 若后续正式切流后需要细化回滚，再补充数据回滚方案。

## 变更日志
- 2026-03-23: `team_members` 新增 `display_name`，用于承载每支队伍内独立的成员称呼，并与入队申请表单语义打通。
- 2026-03-22: `sms_verification_codes` 新增服务端验证码摘要约束，手机号登录改为本地摘要核验，不再依赖 provider 二次校验。
- 2026-03-22: 明确后台关系型管理写链路覆盖 `team_join_requests`、`team_members`、`tournament_participants`、`match_rosters` 与场次赛果/状态推进。
- 2026-03-22: 恢复 Web 管理后台现行 `admin_*` 数据模型，补齐管理员、后台 refresh token 与审计日志约束。
- 2026-03-22: 新增手机号身份表与验证码表规划，补充手机号登录的数据模型约束。
- 2026-03-22: 现行目标态切换为新购杭州 `RDS PostgreSQL Serverless` 关系型表模型，Web 管理后台与 `admin_*` 数据模型纳入现行范围。
- 2026-03-22: 新增 `TestFlight` 共用正式数据库的测试账号与命名规范约束。
- 2026-03-23: 队伍模型补充 `created_at` 的客户端与契约口径说明，支持 iOS 多队场次编辑的默认排序。
- 2026-03-10: 补充账号删除时释放 `apple_sub` 绑定，支持同一 Apple 账号重新注册新账号。
- 2026-03-06: 为账号删除新增 `users.deleted_at` 与 refresh token 撤销约束。
- 2026-02-17: 迁移并纳入根目录统一文档体系。

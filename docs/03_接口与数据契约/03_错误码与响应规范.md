# 错误码与响应规范

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.2
**日期**: 2026-02-23

## 1. 响应结构
1. 错误响应统一为：`{ code, message, requestId, details? }`。
2. 列表响应统一为：`{ items, nextCursor }`。
3. 写接口默认返回“最新对象快照”。

## 2. 错误码分层
1. 鉴权类：`UNAUTHORIZED`、`INVALID_TOKEN`、`APPLE_TOKEN_INVALID`、`DEBUG_TOKEN_DISABLED`、`TEST_LOGIN_DISABLED`。
2. 权限类：`FORBIDDEN`、`TEAM_ROLE_FORBIDDEN`。
3. 冲突类：`CONFLICT`、`DUPLICATE_PENDING_REQUEST`。
4. 校验类：`VALIDATION_ERROR`。
5. 资源类：`NOT_FOUND`。

## 3. 时间与标识
1. 时间字段统一使用 UTC ISO8601。
2. 主键统一 UUID（36 位字符串）。
3. 对外展示标识统一 `public_id`。

## 4. 变更规则
1. 新增错误码必须更新本文件与 API 契约文档。
2. 不允许复用旧错误码表达新语义。
3. 删除错误码前必须确认客户端已完成兼容。

## 5. 变更日志
- 2026-02-23: 新增测试鉴权错误码 `TEST_LOGIN_DISABLED`，用于受控测试手机号登录开关拦截。
- 2026-02-20: 增补 `APPLE_TOKEN_INVALID`、`DEBUG_TOKEN_DISABLED`，明确发布环境鉴权错误语义。
- 2026-02-17: 初始化错误码与响应规范。
- 2026-02-17: 迁移并纳入根目录统一文档体系。

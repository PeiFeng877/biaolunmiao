# 单仓发版执行规范

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.8
**日期**: 2026-03-22
**适用范围**: 主仓 `main` 分支下的 iOS 提审、后端上线、Web 正式发布与联合发版

## 1. 目标
1. 固化单仓模式下的发版入口，避免“代码已改完，但不知道该怎么发”。
2. 让 Git 操作、测试门禁、iOS 提审资料、后端上线步骤都可重复执行。
3. 让产品、研发、后续协作者对每次发版有统一判断口径。

## 1.1 主文档定位
1. 本文件是当前现行唯一发版流程主文档。
2. `docs/04_测试与发布/01_规范/07_中国大陆App备案与AppStore上架一体化手册.md`、`08_中国大陆备案资料包_辩论喵_v1.md`、`09_中国大陆iOS上架备案执行计划.md` 仅作为合规与资料补充，不替代本流程。
3. `docs/04_测试与发布/02_记录/` 下的文档仅用于保存轮次化执行记录，不作为现行流程规范。

## 2. 发布类型
1. `iOS 提审`: 只发 iOS 新包，不要求同日改后端生产逻辑。
2. `后端上线`: 只发后端服务，不要求同日提交 iOS 新包。
3. `Web 正式发布`: 只发 Web 管理后台，不要求同日改 iOS 包。
4. `联合发版`: 同一需求同时改 iOS、后端、Web、文档，并在同一轮发布中落地。

## 3. Git 标准流程
### 3.1 开发分支
1. 所有功能、修复、提审整改都从 `main` 切新分支。
2. 分支命名统一使用 `codex/<topic>`。

```bash
git checkout main
git pull --ff-only origin main
git checkout -b codex/<topic>
```

### 3.2 开发完成后的收口
1. 保证 `git status --short` 为空或只剩本次主题文件。
2. 若接口、数据模型、发布流程变化，必须同步更新 `docs/03_接口与数据契约/` 或 `docs/04_测试与发布/`。
3. 在功能分支先跑完本次需要的最小测试，再合回 `main`。

### 3.3 合回主干
1. 只允许把已经验收通过的功能分支合到 `main`。
2. 默认使用快进合并，避免把无关分支历史揉成一团。

```bash
git checkout main
git merge --ff-only codex/<topic>
```

### 3.4 发版打点
1. 正式发版前，`main` 必须指向本次最终代码。
2. iOS、后端分别打各自可追溯 tag，Web 记录正式 deployment URL。
3. 发版完成后推送 `main` 与 tag，并补齐 Web 正式 deployment 记录。

建议 tag 规则：
1. iOS：`ios/v<版本号>-build<构建号>`
2. 后端：`backend/<YYYY-MM-DD>-<shortsha>`
3. 联合发版：可补一个总 tag，例如 `release/<YYYY-MM-DD>`

```bash
git tag ios/v1.0.0-build5
git tag backend/2026-03-14-78f5e65
git push origin main --tags
```

## 4. 公共发版门禁
1. `git status --short` 为空。
2. 需要回写的文档已经提交到主仓。
3. 关键命令结果可复述，不允许“我本地好像跑过”。
4. 正式发版前，在 `docs/04_测试与发布/02_记录/` 补一份本轮记录，写明版本、提交、测试结果、风险与回滚点。

## 5. iOS 发版流程
### 5.1 发版前必须完成
1. 更新版本号与构建号。
2. 运行治理检查：

```bash
swift bianlunmiao-ios/docs/03_Governance/tools/governance_audit.swift --mode check --root bianlunmiao-ios
```

3. 运行 iOS 单测：

```bash
xcodebuild test -project bianlunmiao-ios/BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.3.1' -only-testing:BianLunMiaoTests CODE_SIGNING_ALLOWED=NO
```

4. 若本轮改了 UI 或关键流程，再补一次 UI 冒烟：

```bash
bash scripts/ios_ui_lane.sh smoke-local
```

5. 补一次真机人工验收，至少覆盖：
   - Apple 登录
   - 手机号验证码登录
   - 首次启动
   - 队伍/赛事/日程/消息关键路径
   - Release 环境网络连通性

### 5.2 提审前必须更新的信息
1. App Store Connect 中的版本号、构建号。
2. 隐私政策 URL。
3. 技术支持 URL。
4. 截图与 1024 图标。
5. 年龄分级、隐私问卷、审核备注。
6. 如涉及中国大陆上架：备案编号、供应信息、App 内备案展示位。
7. 如审核需要复现路径：提供测试账号、操作步骤、审核备注说明。

### 5.3 构建与上传
1. 产物必须来自干净工作区的 `main`。
2. 归档、导出、上传沿用当前工程命令链：

```bash
xcodebuild -project bianlunmiao-ios/BianLunMiao.xcodeproj -scheme BianLunMiao -configuration Release -archivePath build/release/BianLunMiao.xcarchive archive
xcodebuild -exportArchive -archivePath build/release/BianLunMiao.xcarchive -exportOptionsPlist bianlunmiao-ios/build/release/ExportOptions-appstore-connect.plist -exportPath build/release/export -allowProvisioningUpdates
asc builds upload --app 6759727855 --ipa build/release/export/BianLunMiao.ipa --wait
```

3. 上传后必须记录：
   - Git commit
   - 版本号与构建号
   - Archive 路径
   - IPA 路径
   - App Store Connect Build ID

### 5.4 TestFlight 发布补充
1. 若本轮目标是 TestFlight，而不是直接提审，优先使用：

```bash
asc publish testflight --app 6759727855 --ipa build/release/export/BianLunMiao.ipa --group "Internal Testers" --wait
```

2. 历史被拒版本不得直接复用旧 build 重新分发，必须生成新 build。
3. 上传后需要补齐 `What to Test`、测试账号与正式后端复用说明。

## 6. 后端发版流程
### 6.1 发版前必须完成
1. 若本轮改了接口或数据模型，先更新 `docs/03_接口与数据契约/`。
2. 若本轮改的是后端，运行后端代码检查：

```bash
make lint
```

3. 若本轮改的是后端，运行后端发布前测试：

```bash
uv run --extra dev python -m pytest tests/test_mvp_api.py -q
uv run --extra dev python -m pytest tests/test_admin_api.py -q
```

4. 若本轮改的是正式后端，先在本地完成 `GET /healthz` 与 `POST /api` 冒烟，再准备 FC 默认域名部署。

5. 迁移阶段说明：
   - 现行目标后端为 `FC Web 函数 + FastAPI`。
   - 历史 `EMAS` 相关实现仅保留为历史参考；旧 `辩论喵-后端-serverless/` 源码与自动化副本只允许落在本机 `./.data/local-archive/`，不再作为现行发版入口。
   - 正式环境先使用 FC 默认域名承接流量，`api.bianlunmiao.top` 本轮不切。

### 6.2 正式环境流程
1. 先完成本机联调和最小回归，再部署正式后端。

2. 正式环境先用 FC 默认域名，至少覆盖：
   - `GET /healthz -> 200`
   - `auth.debug_token -> 403 DEBUG_TOKEN_DISABLED`
   - 真机 Apple 登录或手机号验证码登录成功并进入空数据主容器
   - 头像上传成功且 `OSS` 可见文件
3. 若有数据结构调整，先确认当前是否允许“空库重建”；进入真实流量阶段后再补充细化回滚策略。
4. 发布完成后，立即检查正式入口、关键接口响应、应用日志与错误率。

### 6.3 必须记录的信息
1. Git commit 与 tag。
2. 发布环境：`prod`。
3. 是否执行数据库迁移。
4. 健康检查结果。
5. 回滚点。

## 7. Web 正式发布流程
### 7.1 发版前必须完成
1. 若本轮改了后台契约、数据模型或正式接入方式，先更新 `docs/03_接口与数据契约/` 与相关架构文档。
2. 确认 `bianlunmiao-admin/.vercel/project.json` 已绑定正式项目，而不是历史 `stg` 项目。
3. 确认本机 `vercel` CLI 登录有效；若 token 失效，先重新登录或更新 token，再执行正式部署。
4. 运行 Web 门禁：

```bash
cd bianlunmiao-admin
pnpm lint
pnpm build
```

### 7.2 正式环境配置
1. Vercel prod 环境变量至少包含：
   - `NEXT_PUBLIC_APP_ENV=prod`
   - `NEXT_PUBLIC_API_BASE_URL=/api/proxy`
   - `INTERNAL_API_BASE_URL=<FC 默认域名根地址>`
2. 浏览器侧统一走 `/api/proxy/healthz` 与 `/api/proxy/rpc`，不直连后端公网地址。

### 7.3 正式部署与验收
1. 产物必须来自干净工作区的 `main`。
2. 正式部署命令：

```bash
vercel deploy --prod --cwd bianlunmiao-admin
```

3. 部署后至少覆盖：
   - `/api/proxy/healthz -> 200`
   - 管理员登录成功
   - 用户、队伍、赛事、场次工作台至少各走一条主路径
4. 必须记录 deployment URL、目标 project、关键环境变量是否已同步。

## 8. 联合发版额外要求
1. iOS 与后端同时改动时，先确认 `docs/03_接口与数据契约/` 已同步。
2. 联合发版默认顺序：
   - 后端本地联调
   - iOS / Web 联调验证
   - 后端正式环境
   - Web 正式部署
   - iOS 正式 Archive / 上传 / TestFlight 或提审
3. 禁止先提审 iOS，再临时补后端破坏性改动。
4. 若 Web 依赖后端新能力，必须以后端正式 smoke 通过后的 `FC_URL` 作为唯一正式入口。

## 9. 回滚规则
### 9.1 iOS
1. 已上传的错误构建不回写覆盖，只能出新 build。
2. 若提审后发现问题，基于 `main` 新建修复分支，修完后重新归档上传。

### 9.2 后端
1. 应用层优先回滚到上一个稳定版本。
2. 数据层优先执行已验证的 `downgrade`；若不可逆，必须先恢复服务再补救数据。
3. 出现 P0/P1 事故时，先止血，再补复盘记录。

### 9.3 Web
1. 优先回滚到上一个稳定 deployment。
2. 若问题来自错误环境变量，先修正 prod 配置再重新部署，不在页面内加临时环境切换开关。

## 变更日志
- 2026-03-22: 将 Web 管理后台纳入现行发版主流程，新增 Vercel prod 配置、部署与回滚口径，并把后端测试门禁显式补齐 `tests/test_admin_api.py`。
- 2026-03-22: 将登录验收口径补充为 Apple 与手机号验证码并列，避免发版门禁仍按 Apple-only 执行。
- 2026-03-22: 收口后端提交与发版主路径，明确旧 EMAS 过渡目录与技能只允许保留本机忽略归档副本，不再占用主仓现行目录。
- 2026-03-22: 将后端发版流程切换为 `FC + FastAPI + 新 RDS`，移除 EMAS/stg 相关门禁与脚本入口，并把对应历史资料迁入 `docs/05_历史归档/05_阿里云阶段资料/`。
- 2026-03-20: 记录 EMAS 迁移期的 `prod` rehearsal 与双环境门禁；该阶段现已冻结，仅保留为历史背景。
- 2026-03-19: 更新 iOS 单测模拟器 runtime 为 `iOS 26.3.1`，并修正 TestFlight 导出所使用的 `ExportOptions` 路径。
- 2026-03-17: 修正主仓根目录下的治理检查命令，统一通过 `--root bianlunmiao-ios` 指向 iOS 模块。
- 2026-03-17: 明确本文件为现行唯一发版流程主文档，并收敛重复的旧版发布规范引用。
- 2026-03-14: 新建单仓发版执行规范，统一 Git 流程、iOS 提审、后端上线与联合发版口径。

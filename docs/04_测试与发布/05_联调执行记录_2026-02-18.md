# 联调执行记录（2026-02-18）

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.1
**日期**: 2026-02-20

## 1. 本轮目标
- 后端本地服务与 iOS 联调链路保持可用。
- iOS 在连接后端改造后完成构建、单测与 UI 冒烟验证。
- 输出可复用的本地验收口径与已知风险。

## 2. 本轮代码改动
### iOS
- `/Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-ios/BianLunMiaoUITests/BianLunMiaoUITests.swift`
  - 加强日程相关 UI 测试的抗抖动能力（等待策略、元素查询方式、回退路径）。
  - 修复 `testScheduleMonthToDayDetailAndSourceManagement`、`testScheduleTodayAndWeekTimelineSwipe`、`testScheduleSyncSheetSelectionAndScroll` 在高负载下的脆弱断言。

## 3. 验证结果
### 3.1 后端（目录：`/Users/Icarus/Documents/project/bianlunmiao/辩论喵-后端`）
- `uv run --python 3.11 --extra dev ruff check .`：通过。
- `uv run --python 3.11 --extra dev pytest -q`：通过（`5 passed`）。

### 3.2 iOS 构建与单测（目录：`/Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-ios`）
- `xcodebuild -project BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2' build`：通过。
- `xcodebuild -project BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2' -only-testing:BianLunMiaoTests test`：通过（`TEST SUCCEEDED`）。

### 3.3 iOS UI 冒烟
- 关键路径冒烟集（消息 / 我的 / 赛事 / 日程）通过：
  - `BianLunMiaoUITests/testMessageTabShowsFlatFeedAndCanOpenJoinRequestDetail`
  - `BianLunMiaoUITests/testMySettingsCanEditNickname`
  - `BianLunMiaoUITests/testTournamentFlowFromListToMatchCreation`
  - `BianLunMiaoUITests/testScheduleMonthToDayDetailAndSourceManagement`
  - `BianLunMiaoUITests/testScheduleSyncSheetSelectionAndScroll`
  - `BianLunMiaoUITests/testScheduleTodayAndWeekTimelineSwipe`
- 全量 `-only-testing:BianLunMiaoUITests` 在当前机器存在不稳定：
  - 现象 A：并行运行时日程用例偶发失败（模拟器高负载场景）。
  - 现象 B：串行一次出现 `Invalid connectionUUID specified`（模拟器运行时基础设施错误，非业务断言失败）。

## 4. 当前验收口径（建议）
在当前环境下，建议先采用“单测全绿 + UI 冒烟用例集”作为联调门禁：

1. 后端门禁
- `uv run --python 3.11 --extra dev ruff check .`
- `uv run --python 3.11 --extra dev pytest -q`

2. iOS 门禁
- `xcodebuild -project BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2' build`
- `xcodebuild -project BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2' -only-testing:BianLunMiaoTests test`
- `xcodebuild -project BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2' -only-testing:BianLunMiaoUITests/testMessageTabShowsFlatFeedAndCanOpenJoinRequestDetail -only-testing:BianLunMiaoUITests/testMySettingsCanEditNickname -only-testing:BianLunMiaoUITests/testTournamentFlowFromListToMatchCreation -only-testing:BianLunMiaoUITests/testScheduleMonthToDayDetailAndSourceManagement -only-testing:BianLunMiaoUITests/testScheduleSyncSheetSelectionAndScroll -only-testing:BianLunMiaoUITests/testScheduleTodayAndWeekTimelineSwipe test`

## 5. 遗留风险
- 全量 UI 自动化在当前模拟器环境仍有偶发不稳定，不建议在本机将其作为唯一阻断门禁。
- 若需要“全量 UI 必须稳定全绿”，建议后续将 UI 套件迁移到固定 CI 设备池，并配套测试计划（禁并行/拆分 LaunchPerformance）。

## 变更日志
- 2026-02-20: 补充文档协议头、版本、日期与变更日志字段，满足文档基线要求。

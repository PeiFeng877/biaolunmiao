# 单仓稳态收口与 TestFlight 发布记录

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.0
**日期**: 2026-03-19
**适用范围**: 主仓 `main`、`bianlunmiao-ios/`、`bianlunmiao-admin/`、`辩论喵-后端/`

## 1. 发布目标
1. 完成单仓稳态收口，按主题提交整理后快进合入 `main`。
2. 在 `main` 上完成治理检查、后端门禁、Admin 构建、iOS 单测与 UI 自动化验收。
3. 发布 iOS `1.0.1 (101)` 到 TestFlight 内部测试组。

## 2. Git 收口结果
### 2.1 主干状态
1. 集成分支：`codex/stabilize-monorepo-20260319`
2. 合入方式：`git merge --ff-only codex/stabilize-monorepo-20260319`
3. 合入后主干提交：`883b858420f52f8b7a42ebeb0ab072f0ba348843`

### 2.2 本轮主题提交
1. `0aa6e3c docs: governance 收口单仓边界规范`
2. `3e4d8f0 fix: iOS stabilize runtime overrides and debug routing`
3. `8ee3575 fix: backend 收口 Apple 校验与赛事可见性`
4. `2d3b0e8 feat: add admin web console module`
5. `539bc0d chore: drop admin build backup artifacts`
6. `2245a61 feat: iOS UI automation lane split and governance refactor`
7. `d6bbb3d docs: archive staging apple login hotfix record`
8. `883b858 chore: iOS refresh runtime baseline and bump build 101`

## 3. 验收结果
### 3.1 治理与后端
1. `swift bianlunmiao-ios/docs/03_Governance/tools/governance_audit.swift --mode check --root bianlunmiao-ios`：通过
2. `cd 辩论喵-后端 && make lint`：通过
3. `cd 辩论喵-后端 && make test`：通过，`23 passed`

### 3.2 Web 管理后台
1. `cd bianlunmiao-admin && pnpm lint`：通过
2. `cd bianlunmiao-admin && pnpm build`：通过

### 3.3 iOS 自动化
1. `xcodebuild test -project bianlunmiao-ios/BianLunMiao.xcodeproj -scheme BianLunMiao -destination 'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.3.1' -only-testing:BianLunMiaoTests CODE_SIGNING_ALLOWED=NO`：通过
2. `bash scripts/ios_ui_lane.sh smoke-local`：通过
3. `bash scripts/ios_ui_lane.sh full-local`：通过
4. `BLM_TEST_REMOTE_BASE_URL=http://120.55.115.147/api/v1 bash scripts/ios_ui_lane.sh stg-smoke`：通过

### 3.4 人工验收说明
1. 真机人工验收未由自动化代理执行。
2. Apple 登录、首次启动、关键业务路径与 Release 网络连通性仍应由持机人员补一次人工确认。

## 4. TestFlight 发布结果
1. App ID：`6759727855`
2. 版本号：`1.0.1`
3. 构建号：`101`
4. Archive 路径：`build/release/BianLunMiao-1.0.1-build101-20260319.xcarchive`
5. IPA 路径：`build/release/export/BianLunMiao.ipa`
6. Build ID：`b81db4a9-5836-418e-89f5-99c815bace68`
7. TestFlight 组：`Internal Testers`
8. 处理状态：`VALID`

## 5. 已知风险
1. Xcode 归档与单测阶段仍有 `Info-Debug.plist` / `Info-Release.plist` 被包含在 `Copy Bundle Resources` 的 warning，当前不阻塞构建与上传，但建议后续清理。
2. 模拟器链路已切到当前机器可用的 `iOS 26.3.1`，若团队统一 runtime 继续升级，需要同步更新脚本和现行发版规范。

## 6. 回滚点
1. Git 回滚点：`ddd712c` 之前的 `main`
2. iOS 回滚方式：不覆盖已上传 build，若发现问题基于 `main` 新建修复分支并生成新 build
3. 后端回滚方式：按现行部署流程回退到上一稳定版本，并检查关键接口与健康状态

## 变更日志
- 2026-03-19: 新建本轮单仓收口、验收与 TestFlight 发布记录。

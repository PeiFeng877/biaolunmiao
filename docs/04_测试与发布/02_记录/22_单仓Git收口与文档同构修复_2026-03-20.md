# 单仓 Git 收口与文档同构修复记录

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.0
**日期**: 2026-03-20
**适用范围**: 主仓 `codex/fix-dark-mode-schedule-timeline`、`bianlunmiao-ios/`、`bianlunmiao-admin/`、`辩论喵-后端/`、`docs/`

## 1. 执行目标
1. 把主仓根目录收口为唯一 Git 事实源。
2. 删除残留在 `bianlunmiao-ios/` 与 `辩论喵-后端/` 下的独立 `.git/`。
3. 清理当前工作区中所有 `* 2.*` 复制副本与 `* 2` 重复目录。
4. 修复根仓、模块级 `agents.md` 与 SSOT 文档的目录索引、路径和设计令牌同构问题。

## 2. 收口结果
### 2.1 Git 拓扑
1. 已删除 `bianlunmiao-ios/.git`。
2. 已删除 `辩论喵-后端/.git`。
3. 执行 `git -C bianlunmiao-ios rev-parse --show-toplevel` 返回主仓根目录。
4. 执行 `git -C 辩论喵-后端 rev-parse --show-toplevel` 返回主仓根目录。

### 2.2 重复副本清理
1. 已删除工作区内全部 `* 2.*` 重复文件。
2. 已删除 `bianlunmiao-admin/` 下全部 `* 2` 重复目录。
3. 当前执行 `find bianlunmiao-admin bianlunmiao-ios scripts -mindepth 1 \( -name '* 2' -o -name '* 2.*' \)` 无输出。
4. 无后缀主文件继续作为唯一事实源，未从旧副本回滚任何内容。

### 2.3 文档同构
1. 根 `agents.md` 已补充“业务子目录不得保留独立 `.git/`”规则。
2. `docs/agents.md`、`docs/04_测试与发布/agents.md`、`docs/04_测试与发布/02_记录/agents.md` 已补齐 `21_单仓稳态收口与TestFlight发布_2026-03-19.md` 与本记录索引。
3. `bianlunmiao-admin/agents.md` 已移除不存在的 `CLAUDE.md`，补齐 `app/layout.tsx`、`app/page.tsx`、`app/api/proxy/[...path]/route.ts`、`.gitignore`、`.env.example` 与 `lib/` 真实结构。
4. `bianlunmiao-ios/agents.md`、`辩论喵-后端/agents.md` 已修正到当前主仓绝对路径。
5. iOS 设计系统文档已同步到当前 `Theme.swift` 颜色令牌，恢复 SSOT 一致性。

## 3. 验证结果
1. `swift bianlunmiao-ios/docs/03_Governance/tools/governance_audit.swift --mode check --root bianlunmiao-ios`：通过。
2. `pnpm --dir bianlunmiao-admin lint`：通过。
3. `make -C 辩论喵-后端 lint`：通过。
4. `bash scripts/ios_ui_lane.sh smoke-local`：通过。

## 4. 提交与执行说明
1. 文档同构修复提交：`1b96f3e docs: monorepo align governance docs`
2. Git 拓扑收口提交：`d19525b refactor: monorepo drop nested git roots`
3. 删除内层 `.git/` 与清理重复副本属于工作树级操作，无法以文件 diff 直接表达，因此通过空提交为主仓保留审计点。

## 5. 剩余说明
1. 当前分支仍保留一组预先存在的 iOS 功能改动，主要集中在日程暗色模式、全天时间轴与 UI 自动化覆盖上；这些改动不是本次单仓治理清理的实现目标。
2. 本次清理未改动后端 API、数据库 schema 或 Web 管理端业务接口。

## 变更日志
- 2026-03-20: 新建单仓 Git 收口、重复副本清理、文档同构修复与验证记录。

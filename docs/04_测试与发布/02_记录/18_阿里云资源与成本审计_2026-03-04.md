# 阿里云资源与成本审计（2026-03-04）

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.2
**日期**: 2026-03-20
**适用范围**: `docs/04_测试与发布/03_工具/aliyun_cost_audit.py`

## 1. 审计口径
1. 仅统计辩论喵项目资源，不做全账号泛审计。
2. 日成本基准日：`2026-03-03`。
3. 包年包月按日摊销计入。

## 2. 核心结论
1. 当前稳态按量日成本：约 `16.44 元/天`。
2. 加上包年包月摊销后：约 `16.75 元/天`。
3. 最高优先节省项：`stg SAE + stg CLB`，约 `6.17 元/天`。

## 2.1 当前执行状态（2026-03-20）
1. 旧 `stg` 高成本资源已删除：
   - `SAE`: `9922a4c5-70a4-4452-b5c4-b038bb7c1cd7` -> `InvalidAppId.NotFound`
   - `CLB`: `lb-bp10eacwg0q6itc92xp1g` -> `InvalidLoadBalancerId.NotFound`
2. 当前可观测成本结构（旧 stg 删除后）：
   - 主要按量计费项：`prod SAE`、`prod CLB`、`RDS`
   - 小额项：`OSS`、`SLS`
   - 摊销项：`ECS`、`域名`
3. 审核期策略：
   - 保持 `prod` 在线，避免审核访问失败
   - `stg` 统一切到 EMAS Serverless，旧云资源不再保留
   - 暂不执行生产架构改造，待审核通过后再推进

## 3. 已确认在用服务
1. `SAE`: 当前只保留 `prod` 应用，旧 `stg` 已删除。
2. `SLB/CLB`: 当前只保留 `prod` 公网入口，旧 `stg` CLB 已删除。
3. `RDS PostgreSQL`: 1 个实例。
4. `ECS`: 1 台，承接正式域名 HTTPS 入口。
5. `OSS`: 1 个 bucket。
6. `SLS`: SAE 相关日志写入。
7. `域名/DNS`: `bianlunmiao.top`。

## 4. 审计脚本
1. 路径：`docs/04_测试与发布/03_工具/aliyun_cost_audit.py`
2. 用法：
```bash
python3 docs/04_测试与发布/03_工具/aliyun_cost_audit.py --billing-date 2026-03-03
python3 docs/04_测试与发布/03_工具/aliyun_cost_audit.py --billing-date 2026-03-03 --format json
```

## 变更日志
- 2026-03-20: 回写旧 `stg SAE + CLB` 已删除，成本关注项从“关闭节流”切换为“资源彻底退役”。
- 2026-03-04: 新增首版审计结论与自动化脚本入口。
- 2026-03-05: 回写 stg 已关闭、审核期策略与当前成本状态摘要。

# 工程规范与协作约定

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.4
**日期**: 2026-03-22


## 1. 分层规范

- `api/`：仅处理 HTTP 协议、鉴权、参数校验、响应装配。
- `models/`：数据库实体定义。
- `schemas/`：请求/响应 DTO。
- `services/`：业务规则工具与跨域逻辑。
- `db/`：数据库连接、会话、迁移入口。

## 2. API 规范

1. 现行公网契约固定为 `GET /healthz` 与 `POST /api`。
2. 写接口返回最新对象快照。
3. 列表统一：`{ items, nextCursor }`。
4. 统一错误体：`{ code, message, requestId, details? }`。
5. 时间字段统一 UTC ISO8601。

## 3. 错误码规范

- 鉴权：`UNAUTHORIZED`、`INVALID_TOKEN`
- 权限：`FORBIDDEN`、`TEAM_ROLE_FORBIDDEN`
- 业务冲突：`CONFLICT`、`DUPLICATE_PENDING_REQUEST`
- 数据校验：`VALIDATION_ERROR`
- 不存在：`NOT_FOUND`

## 4. 数据规范

1. 主键统一 UUID 字符串（36 位）。
2. 对外展示 ID 统一 `public_id`（用户/队伍）。
3. 使用软状态字段（`status`）描述生命周期。

## 5. Git 与发布规范

1. 分支命名：`codex/<feature-or-fix>`。
2. 提交信息：`feat|fix|docs|refactor: <scope> <summary>`。
3. 禁止直接在生产改配置，必须先经本地联调、FC 默认域名冒烟或 TestFlight 验证。
4. 具体发版执行步骤统一以 `docs/04_测试与发布/01_规范/16_单仓发版执行规范.md` 为准。

## 6. 测试规范

- 新增核心业务规则必须带最少 1 个接口级测试。
- 每次发布前至少验证：认证、队伍申请、场次排阵、赛果录入、消息 ack。

## 变更日志
- 2026-03-22: 将现行 API 规范收口为 FC 默认域名下的单入口 RPC：`GET /healthz` 与 `POST /api`。
- 2026-03-22: 将“预发验证”口径收敛为本地联调、FC 默认域名冒烟与 TestFlight 复用正式后端，不再依赖云端测试服。
- 2026-03-20: 将现行 API 规范从 REST 前缀收口为 EMAS 单入口 RPC：`GET /healthz` 与 `POST /api`。
- 2026-03-17: 在高层工程约定中补充现行发版流程主文档入口，避免与测试发布模块规则分叉。
- 2026-02-17: 迁移并纳入根目录统一文档体系。

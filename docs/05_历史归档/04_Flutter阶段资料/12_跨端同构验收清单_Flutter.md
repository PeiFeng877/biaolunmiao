# 跨端同构验收清单（Flutter）

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.0
**日期**: 2026-03-05
**状态**: 历史归档（2026-03-14 起退出现行 SSOT）
**主机型基线**: iPhone 17 Pro（iOS）/ Pixel 8（Flutter Android）

## 1. 构建与测试门禁
1. `cd /Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-flutter`
2. `flutter analyze`
3. `flutter test`
4. `flutter build apk --debug`
5. 结果要求：命令均成功，APK 可安装。

## 2. 视觉同构验收（亮色 + 深色）
1. 队伍根页与详情页。
2. 赛事根页与详情页。
3. 日程根页与按日详情页。
4. 消息根页与详情页。
5. 我的根页、编辑页、更多页。

验收标准：
1. 色彩、字号、间距、描边、圆角符合同构规范。
2. 容差控制：间距 `<= 2dp`，字号 `<= 1sp`。
3. 深色模式无文本对比失衡或组件错位。

## 3. 交互同构脚本
1. 登录 -> 进入 5 Tab。
2. 队伍：创建队伍、加入队伍、进入队伍详情、返回原 Tab。
3. 赛事：创建赛事、进入赛事详情、返回原 Tab。
4. 日程：来源开关、新增来源、系统日历同步、按日详情进入/返回。
5. 消息：进入消息详情、确认动作状态变更。
6. 我的：进入编辑资料、保存资料、进入更多页、退出登录。

验收标准：
1. 路由层级与返回路径稳定。
2. 弹层开合、按压反馈、列表切换节奏与 iOS 同构。

## 4. 业务冒烟验收
1. 云端测试服连接正常（`API_BASE_URL` 指向阿里云测试服）。
2. 创建队伍、申请加入、创建赛事、日程同步、消息确认、资料更新链路可用。
3. Flutter 操作可被 iOS 侧同账号感知（跨端互通）。

## 5. 结果记录模板
1. 验收时间：
2. 构建包信息（路径/SHA256）：
3. 视觉同构通过率（目标 `>= 95%`）：
4. 交互脚本通过率：
5. 失败项与回滚建议：

## 变更日志
- 2026-03-14: 迁入历史归档，停止作为现行同构验收清单执行依据。
- 2026-03-05: 新建 Flutter 跨端同构验收清单，替换 Android 独立仓阶段文档的现行位置。

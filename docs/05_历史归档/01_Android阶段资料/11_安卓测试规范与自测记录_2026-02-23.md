# 安卓测试规范与自测记录（2026-02-23）

[PROTOCOL]: 变更时更新此头部，然后检查 agents.md

**版本**: v1.3
**日期**: 2026-02-23
**状态**: 历史归档（Android 独立仓阶段执行记录）

## 1. 目标
1. 明确 Android 测试包发布前的最低测试门禁，禁止“未验证直接分发”。
2. 固化本轮自测证据，确保可审计、可复现。
3. 补齐 Android 方向的 AI Skills 与 MCP 工具调研记录。

## 2. Android 最小测试规范（发布前必跑）
1. 后端静态检查与单元测试：
   - `cd /Users/Icarus/Documents/project/bianlunmiao/辩论喵-后端`
   - `make lint`
   - `make test`
2. Android 单元测试与构建：
   - `cd /Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-android`
   - `./gradlew :app:testDebugUnitTest`
   - `./gradlew :app:assembleDebug`
3. 产物校验：
   - `shasum -a 256 /Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-android/app/build/outputs/apk/debug/app-debug.apk`
4. 手工冒烟（至少一轮）：
   - 登录（手机号 11 位 + 任意非空验证码）
   - 队伍（创建队伍、入队申请）
   - 赛事（创建赛事、创建场次）
   - 日程（拉取并尝试同步系统日历）
   - 消息（列表拉取与确认）
   - 我的（昵称与头像更新）
5. 环境约束检查：
   - 启动期校验 `API_BASE_URL` 不得含 `localhost/127.0.0.1`
   - 抓包或日志确认目标为阿里云测试服地址

## 3. 本轮自测执行记录
执行时间：`2026-02-23 15:49:44 CST`

1. 后端验证（通过）：
   - 命令：`make lint && make test`
   - 结果：`ruff check` 通过，`pytest` 通过（`14 passed`）
2. Android 验证（通过）：
   - 命令：`./gradlew :app:testDebugUnitTest :app:assembleDebug`
   - 结果：`BUILD SUCCESSFUL`
3. APK 校验（通过）：
   - 产物：`/Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-android/app/build/outputs/apk/debug/app-debug.apk`
   - SHA256：`028edaea0e3746b14765ec1763ee6d259a0097b5561e07ea7a96c6e27c8fcb94`

## 4. Find Skills 检索与补齐结果
1. 检索命令：
   - `npx -y skills find "android kotlin"`
   - `npx -y skills find "jetpack compose testing"`
   - `npx -y skills find "android mcp"`
2. 已安装技能（全局）：
   - `android-kotlin`（路径：`/Users/Icarus/.agents/skills/android-kotlin`）
   - `compose-performance-audit`（路径：`/Users/Icarus/.agents/skills/compose-performance-audit`）
   - `compose-navigation`（路径：`/Users/Icarus/.agents/skills/compose-navigation`）
3. 生效说明：
   - Skills CLI 提示安装成功；Codex 侧通常需重启会话后，新的 skills 才会进入当前会话清单。

## 5. MCP 工具检查结果
1. `list_mcp_resources` 结果：空（`[]`）
2. `list_mcp_resource_templates` 结果：空（`[]`）
3. 结论：当前会话未接入可直接执行 Android 自动化验证的 MCP 服务，需要后续单独接入。

## 6. 风险与改进项
1. Android Gradle Plugin 8.5.2 对 `compileSdk=35` 给出兼容性告警（不阻断构建）。
2. 测试服公网入口已修复，但 `api-stg.bianlunmiao.top` 仍受 ICP/域名策略影响，当前继续使用 `http://120.55.115.147`。
3. 后续建议补充 `connectedDebugAndroidTest`（真机/模拟器 UI 自动化）作为发布前门禁。

## 7. 真机跟跑补充（2026-02-23 16:00+）
1. 设备连接与安装：
   - `adb devices -l` 已识别真机并授权。
   - `adb install -r .../app-debug.apk` 安装成功。
2. 首次登录失败根因：
   - 日志：`CLEARTEXT communication to 120.55.115.147 not permitted by network security policy`。
   - 修复：Android `Manifest` 增加 `android:usesCleartextTraffic="true"`，并配置 `network_security_config.xml`。
3. 修复后结果：
   - 登录请求已发出：`POST http://120.55.115.147/api/v1/auth/test-phone`。
   - 当前阻塞：`SocketTimeoutException: failed to connect to /120.55.115.147:80 after 20000ms`。
   - 结论：App 侧明文网络策略问题已解决，现阶段阻塞转为“测试服入口连通性/可用性”。
4. 热修复产物：
   - APK：`/Users/Icarus/Documents/project/bianlunmiao/artifacts/android/app-debug-v0.1.1-test.apk`
   - APK SHA256：`c511fa6d005986a38bf2544951ec4e46d2e814f3878c947b11d3fecfa67d42a8`
   - ZIP：`/Users/Icarus/Documents/project/bianlunmiao/artifacts/android/app-debug-v0.1.1-test.zip`
   - ZIP SHA256：`ca9abc8a4cefafc7aba5f1c59a074e3e78dc5cc7a39d6f964a18a1041ca8b6de`

## 8. 云端入口修复结论（2026-02-23）
1. SAE 与网络修复：
   - SAE 应用：`bianlunmiao-backend-stg`（AppId=`9922a4c5-70a4-4452-b5c4-b038bb7c1cd7`）已恢复。
   - SLB 监听：`lb-bp10eacwg0q6itc92xp1g:80` 已恢复 `running`，后端健康 `normal`。
2. 数据库修复：
   - RDS 实例 `pgm-bp1v5t851p7rtl93` 已从 `STOPPED` 启动到 `Running`。
3. 后端发布：
   - 新镜像：`crpi-5yg31t086w4thbmn.cn-hangzhou.personal.cr.aliyuncs.com/bianlunmiao/backend:stg-20260223-testphone01`
   - 已开启环境变量：`ENABLE_TEST_PHONE_LOGIN=true`。
4. 联通与接口验收：
   - `GET http://120.55.115.147/healthz` -> `200 {"ok":true}`
   - `POST /api/v1/auth/test-phone`（任意非空验证码）-> `200`，返回 token bundle。

## 9. 真机复测通过记录（2026-02-23 16:45 CST）
1. 构建与测试（通过）：
   - `JAVA_HOME=/Applications/Android Studio.app/Contents/jbr/Contents/Home ./gradlew :app:assembleDebug`
   - `JAVA_HOME=/Applications/Android Studio.app/Contents/jbr/Contents/Home ./gradlew :app:testDebugUnitTest`
   - 结果：均 `BUILD SUCCESSFUL`。
2. 修复项（通过）：
   - 代码：`/Users/Icarus/Documents/project/bianlunmiao/bianlunmiao-android/app/src/main/java/com/bianlunmiao/android/network/ApiService.kt`
   - 变更：分页默认参数从 `200` 统一为 `100`，消除后端 `limit <= 100` 约束导致的 `422`。
3. 真机登录与首页初始化（通过）：
   - `POST /api/v1/auth/test-phone` -> `200`
   - `GET /api/v1/users/me` -> `200`
   - `GET /api/v1/teams/my?limit=100` -> `200`
   - `GET /api/v1/teams/discover?limit=100` -> `200`
   - `GET /api/v1/messages?limit=100` -> `200`
   - `GET /api/v1/tournaments?limit=100` -> `200`
   - `GET /api/v1/schedule/sources` -> `200`
   - UI 验证：进入 5 Tab（队伍/赛事/日程/消息/我的）。
4. 5 Tab 切页冒烟（通过）：
   - 队伍页可见：`创建队伍`、`我的队伍`。
   - 赛事页可见：`创建赛事`、赛事列表。
   - 日程页可见：`日历同步`、`来源开关`。
   - 消息页可见：`消息` 列表页。
   - 我的页可见：`用户信息`、`保存资料`、`退出登录`。
5. 新分发产物：
   - APK：`/Users/Icarus/Documents/project/bianlunmiao/artifacts/android/app-debug-v0.1.2-test.apk`
   - APK SHA256：`344608de7db317cedf15d8b1e63c092643ffd2bf1b0d0d759eca8b6c62f916d5`
   - ZIP：`/Users/Icarus/Documents/project/bianlunmiao/artifacts/android/app-debug-v0.1.2-test.zip`
   - ZIP SHA256：`4b78ee0d0d553807a9a19bb7e89297caee9fe2c61d50d304b64c0feec7ce9558`
   - ZIP 下载：`https://bianlunmiao-assets-1917380129637610.oss-cn-hangzhou.aliyuncs.com/stg/apk/android/20260223/app-debug-v0.1.2-test.zip`

## 10. UI 同构重构编译验收（2026-02-23）
1. 执行命令：
   - `JAVA_HOME=/Applications/Android Studio.app/Contents/jbr/Contents/Home ./gradlew :app:testDebugUnitTest :app:assembleDebug`
2. 结果：
   - `BUILD SUCCESSFUL`
3. 说明：
   - Android UI 已完成路由拆分（`AppRoute`）、页面分层（`ui/screens`）与设计系统落地（`ui/designsystem`）。
   - 保持现有后端契约不变，编译与单测门禁通过。

## 变更日志
- 2026-02-23: 新增 UI 同构重构编译验收记录（`testDebugUnitTest + assembleDebug` 通过）。
- 2026-02-23: 增补 16:45 真机复测通过记录（修复分页参数 `422`）与 v0.1.2 测试包信息。
- 2026-02-23: 新增 Android 测试规范、自测证据、Find Skills 与 MCP 补齐记录。
- 2026-02-23: 增补真机跟跑记录与登录链路故障定位（明文策略修复后，卡在测试服连接超时）。
- 2026-02-23: 增补云端入口修复结果（RDS/SLB/SAE）与 `test-phone` 在线验收通过记录。

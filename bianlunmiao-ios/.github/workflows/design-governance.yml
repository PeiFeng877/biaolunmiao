name: quick-ci

on:
  push:
    branches-ignore:
      - main
      - master
  pull_request:

concurrency:
  group: quick-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect_changes:
    runs-on: ubuntu-latest
    outputs:
      ui_risk: ${{ steps.filter.outputs.ui_risk }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed scopes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            ui_risk:
              - 'bianlunmiao-ios/BianLunMiao/Views/**'
              - 'bianlunmiao-ios/BianLunMiao/DesignSystem/**'
              - 'bianlunmiao-ios/BianLunMiao/ViewModels/**'
              - 'bianlunmiao-ios/BianLunMiao/Data/**'
              - 'bianlunmiao-ios/BianLunMiao/Models/**'
              - 'bianlunmiao-ios/BianLunMiaoUITests/**'
              - 'bianlunmiao-ios/BianLunMiao.xcodeproj/**'

  guardrail:
    runs-on: macos-latest
    needs: detect_changes

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate governance inventories
        run: swift bianlunmiao-ios/docs/03_Governance/tools/governance_audit.swift --mode check --root bianlunmiao-ios

      - name: Install lint tools
        run: |
          command -v swiftlint >/dev/null || brew install swiftlint
          command -v swiftformat >/dev/null || brew install swiftformat

      - name: SwiftFormat lint
        run: swiftformat bianlunmiao-ios/BianLunMiao bianlunmiao-ios/BianLunMiaoTests bianlunmiao-ios/BianLunMiaoUITests --lint --lenient

      - name: SwiftLint lint
        run: swiftlint lint --config bianlunmiao-ios/.swiftlint.yml

      - name: Run unit tests (quick gate)
        run: |
          DEVICE_NAME=$(xcrun simctl list devices available iOS | grep -E "iPhone" | head -n 1 | sed -E 's/^[[:space:]]*([^()]+) \(.*/\1/' | xargs)
          if [ -z "$DEVICE_NAME" ]; then
            echo "No available iPhone simulator found"
            exit 1
          fi
          xcodebuild test \
            -project bianlunmiao-ios/BianLunMiao.xcodeproj \
            -scheme BianLunMiao \
            -destination "platform=iOS Simulator,name=$DEVICE_NAME" \
            -only-testing:BianLunMiaoTests \
            CODE_SIGNING_ALLOWED=NO

  ui_smoke:
    runs-on: macos-latest
    needs:
      - detect_changes
      - guardrail
    if: ${{ needs.detect_changes.outputs.ui_risk == 'true' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run UI smoke tests (risk-based)
        run: bash scripts/ios_ui_lane.sh smoke-local
